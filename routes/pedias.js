import express from 'express'
import { } from '../controllers/pedias.js'

const router = express.Router()

// 新增文章
// router.post('/', create)
// // 查詢全部文章
// router.get('/', getNew)
// // 刪除指定文章
// router.delete('/:id', delNew)
// // 編輯指定文章
// router.patch('/:id', editNew)

export default router